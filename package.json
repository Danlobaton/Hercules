{
  "name": "hercules",
  "version": "0.8.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev_deploy": "npm install && cd client && npm install && npm run build && cd .. && node app.js",
    "soft_deploy": "cd client && npm run build && cd .. && nodemon app.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemod": "^2.2.5",
    "request": "^2.88.0",
    "request-promise": "^4.2.5"
  },
  "engines": {
    "node": "12.14.0"
  }
}
